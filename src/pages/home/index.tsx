import { Hero } from "../../components/Hero";
import { DefaultLayout } from "../../_layouts/default";

export const Home = () => {
  return (
    <DefaultLayout>
      <Hero />
    </DefaultLayout>
  );
};
